# Debrief Log, `<YOUR NAME>`

> A running log of debriefs: after meetings, after work sessions, after anything worth capturing. Newest at the top. The debrief skill appends here AND files the pieces (tasks, decisions, follow-ups) into the right files. This log is the audit trail of what you processed.

---

## `<date>`, `<what was debriefed, e.g. "board call" / "afternoon work block">`

**Summary:** `<2-3 lines>`

**Filed:**
- Tasks → `<where>`
- Decisions → `<where>`
- Waiting-for → `<where>`
- Brain updates → `<where>`

**Loose threads not yet filed:**
- `<anything that needs a human decision before it can be filed>`

---

## `<date>`, `<next>`

**Summary:**
**Filed:**
**Loose threads:**

---

### Why this exists separately from the meeting notes

Meeting notes are per-meeting and live with the project. This log is your single chronological record of everything you have processed, across all projects. When you wonder "did I ever deal with that?", this is where you look. It is also what the reflect skill reads to see your week as a whole.
